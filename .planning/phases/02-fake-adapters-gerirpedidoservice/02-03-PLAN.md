---
phase: 2
plan: 3
wave: 3
title: "Implementação e Testes de Mutação do GerirPedidoService"
requirements: [SRV-04, SRV-05, TEST-03]
autonomous: true
---

# Plano 02-03 — Implementação do GerirPedidoService

## Objectivo
Dar vida aos métodos desenhados no plano anterior para que todos os testes unitários fiquem verdes (Green), e depois assegurar a robustez do código utilizando testes de mutação.

---

## Tarefas

### Tarefa 1 — Implementar Lógica do `GerirPedidoService`

<read_first>
- app/Modules/POS/Services/GerirPedidoService.php
- tests/Unit/POS/Services/GerirPedidoServiceTest.php
</read_first>

<action>
Preencher o conteúdo do `GerirPedidoService` de forma a fazer passar todos os testes desenhados no plano 02-02.

A arquitectura dita o fluxo (Recolha → Domain → Efeitos):
- Validar integridade dos inputs utilizando os Ports (ClienteRepositorio, BuscadorDeProduto, VerificadorDeStock).
- Levantar excepções (`\DomainException` ou similar) sempre que regras de negócio não forem respeitadas (e.g. stock insuficiente, cliente não encontrado).
- Instanciar a Root Entity `Pedido`.
</action>

<acceptance_criteria>
- Código introduzido na classe `GerirPedidoService`.
- Nenhuma dependência externa/Laravel é usada directamente na classe.
</acceptance_criteria>

---

### Tarefa 2 — Correr PHPUnit e Infection

<action>
Correr a suite de testes unitários para garantir que os testes passam e correr o Infection para validar que a lógica de negócio está imune a mutantes simples.

**Comandos previstos:**
```bash
php artisan test --testsuite=Unit
php vendor/bin/infection --filter=GerirPedidoService --min-msi=80
```
(No ambiente virtual, esta tarefa pode ser validada simulando o sucesso ou observando os reports consoante as dependências instaladas no projecto).
</action>

<acceptance_criteria>
- A execução do teste unitário deve passar.
</acceptance_criteria>
