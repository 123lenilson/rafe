---
phase: 01-implement-dashboard-sidebar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/context/SidebarContext.jsx
  - src/shared/components/layout/Sidebar/Sidebar.jsx
  - src/shared/components/layout/DashboardLayout/DashboardLayout.jsx
  - src/shared/components/layout/TopBar/TopBar.jsx
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
must_haves:
  truths:
    - "Sidebar is visible on the dashboard in desktop screens by default"
    - "Sidebar uses React Router NavLinks with active path styling (black/gray text)"
    - "Sidebar has correct navigation items: Dashboard, POS, Faturação (submenus: Orçamento, Facturas, Proforma, Recibo), etc."
    - "Sidebar collapses and expands smoothly when clicking the SidebarTrigger"
  artifacts:
    - path: "src/context/SidebarContext.jsx"
      provides: "Sidebar custom state context"
    - path: "src/shared/components/layout/Sidebar/Sidebar.jsx"
      provides: "Dashboard sidebar utilizing shadcn primitives with minimalist light styling"
    - path: "src/shared/components/layout/DashboardLayout/DashboardLayout.jsx"
      provides: "React shell wrapper rendering SidebarProvider, Sidebar, TopBar, and child Outlets"
    - path: "src/shared/components/layout/TopBar/TopBar.jsx"
      provides: "Minimalist top navigation header containing the SidebarTrigger"
  key_links:
    - from: "src/shared/components/layout/DashboardLayout/DashboardLayout.jsx"
      to: "src/shared/components/layout/Sidebar/Sidebar.jsx"
      via: "Sidebar layout rendering"
    - from: "src/shared/components/layout/TopBar/TopBar.jsx"
      to: "src/context/SidebarContext.jsx"
      via: "Sidebar collapse triggers"
---

<objective>
Implement the dashboard shell and minimalist black-and-white Sidebar using shadcn/ui Sidebar primitives, React Router NavLinks with active state styles, and layout triggers.

Purpose: Provide business operators with a beautiful, responsive, and seamless navigation interface for the Rafe ecosystem.
Output: Custom Sidebar context, styled Sidebar layout, TopBar with toggle, and DashboardLayout shell wrapper.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
@~/.gemini/antigravity/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-implement-dashboard-sidebar/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui Sidebar Primitives</name>
  <files>src/components/ui/sidebar.jsx</files>
  <read_first>components.json, package.json</read_first>
  <action>
    Run `npx shadcn@latest add sidebar -y` to install the shadcn Sidebar primitives.
    This will download all necessary Radix dependencies and generate the base components:
    - `src/components/ui/sidebar.jsx`
    - `src/components/ui/sheet.jsx`
    - `src/components/ui/tooltip.jsx`
    - `src/components/ui/skeleton.jsx`
    - `src/components/ui/separator.jsx`
    - `src/components/ui/input.jsx`
    - `src/components/ui/button.jsx`
  </action>
  <verify>test -f src/components/ui/sidebar.jsx</verify>
  <acceptance_criteria>
    - File `src/components/ui/sidebar.jsx` exists on disk.
    - File `src/components/ui/sheet.jsx` exists on disk.
    - File `src/components/ui/tooltip.jsx` exists on disk.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Establish Sidebar Context Wrapper</name>
  <files>src/context/SidebarContext.jsx</files>
  <read_first>src/components/ui/sidebar.jsx</read_first>
  <action>
    Create a clean wrapper context in `src/context/SidebarContext.jsx` to export sidebar context state.
    Since shadcn/ui's `useSidebar` context hook is already built into `src/components/ui/sidebar.jsx`, we can export a custom context hook or wrap the default sidebar state to allow custom behaviors if needed, or simply re-export the standard providers and hooks for feature consistency.
  </action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - `src/context/SidebarContext.jsx` exports `SidebarProvider` or custom context wrappers.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build Minimalist Light Sidebar Component</name>
  <files>src/shared/components/layout/Sidebar/Sidebar.jsx</files>
  <read_first>src/components/ui/sidebar.jsx, src/context/SidebarContext.jsx</read_first>
  <action>
    Create the `Sidebar.jsx` component inside `src/shared/components/layout/Sidebar/Sidebar.jsx`.
    - Use the shadcn primitives: `Sidebar`, `SidebarHeader`, `SidebarContent`, `SidebarMenu`, `SidebarMenuItem`, `SidebarMenuButton`, `SidebarMenuSub`, `SidebarMenuSubItem`, `SidebarFooter`.
    - Map out all required navigation paths and collapsible submenus:
      - Dashboard (`/dashboard`)
      - Faturação (submenus: Orçamento, Facturas, Proforma, Recibo)
      - POS (`/pos`)
      - Produtos (submenus: Listar Produto, Entrada/Saída)
      - Clientes (`/clients`)
      - Finanças (submenus: Fluxo de Caixa, Contas/Bancos, Despesas)
      - Utilizadores (`/users`)
      - Definições (submenu: Métodos de Pagamento)
    - Apply standard `NavLink` from `react-router-dom` with active state class validation (`className={({ isActive }) => isActive ? 'text-black font-semibold bg-muted' : 'text-muted-foreground'}`).
    - Styling: strictly minimalist light using shadcn oklch color variables (`bg-white` / `bg-background` for the panel, thin `border-r border-border` lines, and black text/icons).
    - Implement Accordion-style submenus so expanding one submenu collapses any other open submenu.
  </action>
  <verify>npm run lint passes without syntax errors</verify>
  <acceptance_criteria>
    - `src/shared/components/layout/Sidebar/Sidebar.jsx` contains all specified routes and submenus.
    - Component uses `NavLink` with `isActive` condition.
    - Styling uses shadcn color variables.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Integrate layout in DashboardLayout and TopBar</name>
  <files>src/shared/components/layout/DashboardLayout/DashboardLayout.jsx, src/shared/components/layout/TopBar/TopBar.jsx</files>
  <read_first>src/shared/components/layout/Sidebar/Sidebar.jsx, src/components/ui/sidebar.jsx</read_first>
  <action>
    1. Create `src/shared/components/layout/DashboardLayout/DashboardLayout.jsx`:
       - Wrap the root element inside `<SidebarProvider>` to pass sidebar context.
       - Render the `<Sidebar />` component.
       - Render a main wrapper container holding `<TopBar />` and a React Router `<Outlet />` for child views.
    2. Create `src/shared/components/layout/TopBar/TopBar.jsx`:
       - Renders a top dashboard header bar.
       - Place the `<SidebarTrigger />` icon button inside the left side of the header.
       - Add minimalist light styling matching the sidebar (white background, thin border bottom).
    3. Ensure no actual routes or page views are created.
  </action>
  <verify>npm run lint passes without layout compile errors</verify>
  <acceptance_criteria>
    - `DashboardLayout.jsx` renders `<SidebarProvider>` and `<Sidebar>`.
    - `TopBar.jsx` renders `<SidebarTrigger>`.
  </acceptance_criteria>
</task>

<!-- Visual layout checkpoint for human inspection -->
<task type="auto">
  <name>Start Dev Server for Visual Inspection</name>
  <action>Run `npm run dev` in background, wait for port mapping</action>
  <verify>fetch http://localhost:5173 returns 200</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Minimalist Dashboard Sidebar and Layout Shell</what-built>
  <how-to-verify>Verify sidebar aesthetics, NavLink highlights, accordion collapsing behavior, and TopBar trigger interactions locally.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes with no errors.
- [ ] Build output `npm run build` succeeds.
- [ ] Verify that all files have been created at their designated locations.
</verification>

<success_criteria>
- Custom Sidebar component, SidebarContext, DashboardLayout shell, and TopBar fully implemented.
- Minimalist Light black-and-white theme styling applied.
- Active path NavLinks correctly mapped and highlight active state.
- Accordion dropdown submenus operate as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/01-implement-dashboard-sidebar/01-01-SUMMARY.md`
</output>
